from django.http import JsonResponse
from django.shortcuts import render



# password strength checker LOgic over here

# ENDPOINT :  POST /password-strength 

def password_strength(request):
    
    return JsonResponse({})
